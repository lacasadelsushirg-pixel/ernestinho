{
  "private": true,
  "scripts": {
    "check:syntax": "node scripts/validate-rebuild.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "esbuild": "^0.25.10"
  }
}
