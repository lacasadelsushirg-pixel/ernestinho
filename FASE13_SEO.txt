ERNESTINHO CARIOCA — FASE 13 SEO · DATOS ESTRUCTURADOS JSON-LD

Implementado:
- Organization: identidad base Ernestinho Carioca.
- WebSite: entidad principal del sitio.
- BreadcrumbList: generado dinámicamente para rutas internas.
- Article: únicamente para /articulos/:slug.
- Un único bloque JSON-LD administrado dinámicamente al cambiar de ruta.
- No se añadieron tipos Restaurant, TouristAttraction, Event ni LocalBusiness de forma automática: requieren una auditoría de campos específicos antes de declararlos.

Principio aplicado:
El Schema describe solamente información que la aplicación ya conoce. No se inventaron teléfonos, direcciones, precios, autores, fechas ni ratings.

Validación técnica:
- app.js: Node --check OK
- data-core.js: Node --check OK
- gastronomy-data.js: Node --check OK
- No se modificó el diseño visual.
- No se publicó en GitHub/Vercel.

Pendiente para auditoría final:
- validar el JSON-LD ya desplegado con herramientas de Google;
- comprobar deep-links por HTTP en producción;
- evaluar schemas específicos solamente donde los datos sean suficientes.
